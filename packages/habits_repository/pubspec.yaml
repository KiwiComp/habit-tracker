name: habits_repository
description: Local persistence for habits and their completion history.
version: 0.1.0+4
publish_to: none

environment:
  sdk: ^3.12.0
  flutter: ^3.44.0

resolution: workspace

dependencies:
  drift: ^2.34.3
  drift_flutter: ^0.3.1
  flutter:
    sdk: flutter
  meta: ^1.19.0
  uuid: ^4.6.0

dev_dependencies:
  async: ^2.13.1
  build_runner: ^2.15.1
  drift_dev: ^2.33.0
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.5
  very_good_analysis: ^10.3.0
