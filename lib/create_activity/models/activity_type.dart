/// The kind of activity a user can add: a recurring habit or a one-off
/// task.
enum ActivityType {
  /// A recurring activity, done on a repeating schedule.
  habit,

  /// A one-off activity, done a single time.
  task,
}
